bindsym {
    # Terminal
    $super+Return exec kitty
    #$super+Return exec ptyxis --new-window
    
    # Browser
    $super+z exec brave-browser

    # Filemanager
    $super+x exec thunar

    # Screenshot (to clipboard)
    Print exec grim -g "$(slurp)" - | wl-copy --type image/png
}